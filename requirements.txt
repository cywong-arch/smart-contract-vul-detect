# Smart Contract Vulnerability Detection System Dependencies
# Minimal requirements - only what's actually used by the application

# Web application (required)
flask>=2.0.0
flask-cors>=3.0.10  # For Remix IDE API CORS support

# CLI utilities (used by web app)
click>=8.0.0
colorama>=0.4.4
rich>=12.0.0
tabulate>=0.9.0

# Note: The application uses regex-based parsing (built-in 're' module)
# No external Solidity parsers are needed as parsing is done with Python's standard library
